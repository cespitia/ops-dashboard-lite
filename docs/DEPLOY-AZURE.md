# Azure Deployment (TODO)

Planned:
- Deploy Blazor Server to Azure App Service (.NET)
- Use Azure SQL Database
- Configure connection string via:
  ConnectionStrings__DefaultConnection
- Restrict CORS/HTTPS settings as needed